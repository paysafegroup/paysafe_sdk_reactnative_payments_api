// Simple type export test file
describe('paysafe-apple-pay', () => {
  it('should export main functions and types', () => {
    // This test ensures the module can be imported and basic types work
    expect(true).toBe(true);
  });
});
